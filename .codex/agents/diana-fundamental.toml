# 생성 파일 — sync_agent_compat.py 가 .claude/agents/diana-fundamental.md 에서 만든다. 손으로 고치지 않는다.
# ⚠️ 형식 미확인: Codex 설치본이 이 파일을 어떤 스키마로 읽는지 이 세션에서 공식 문서로 확인하지 못했다(2026-09-10).
#    참고용이다. 확인되면 sync_agent_compat.py 의 agent_toml() 을 그 스키마로 고치고 다시 생성한다.
#    model 키가 없는 것은 의도다 — 세션 기본값 상속, 자동 배정·라우터 없음.
name = "diana-fundamental"
description = "주식의 재무·기본적 분석(재무제표/밸류에이션/수익성/성장성)을 담당하는 애널리스트. 기업 가치 분석이 필요할 때 사용."
instructions_file = ".claude/agents/diana-fundamental.md"
tools = ["WebSearch", "WebFetch", "Bash"]
read_only = true
