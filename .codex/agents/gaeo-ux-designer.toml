# 생성 파일 — sync_agent_compat.py 가 .claude/agents/gaeo-ux-designer.md 에서 만든다. 손으로 고치지 않는다.
# ⚠️ 형식 미확인: Codex 설치본이 이 파일을 어떤 스키마로 읽는지 이 세션에서 공식 문서로 확인하지 못했다(2026-09-10).
#    참고용이다. 확인되면 sync_agent_compat.py 의 agent_toml() 을 그 스키마로 고치고 다시 생성한다.
#    model 키가 없는 것은 의도다 — 세션 기본값 상속, 자동 배정·라우터 없음.
name = "gaeo-ux-designer"
description = "GAEO의 PC/모바일 사용성·정보 구조·가독성·디자인 일관성을 검토하는 디자이너. 화면 개편, 새 UI 추가, 디자인 리뷰가 필요할 때 사용한다. docs/gaeo_design_system.md의 규칙(분석가별 고유색 금지·장식 그라데이션 금지·빨강파랑은 시장 방향에만 등)을 지키는지 감시하는 역할도 겸한다."
instructions_file = ".claude/agents/gaeo-ux-designer.md"
tools = ["Read", "Grep", "Glob", "Bash"]
read_only = true
