# 생성 파일 — sync_agent_compat.py 가 .claude/agents/chief-pm.md 에서 만든다. 손으로 고치지 않는다.
# ⚠️ 형식 미확인: Codex 설치본이 이 파일을 어떤 스키마로 읽는지 이 세션에서 공식 문서로 확인하지 못했다(2026-09-10).
#    참고용이다. 확인되면 sync_agent_compat.py 의 agent_toml() 을 그 스키마로 고치고 다시 생성한다.
#    model 키가 없는 것은 의도다 — 세션 기본값 상속, 자동 배정·라우터 없음.
name = "chief-pm"
description = "기술적(TARO)·재무(DIANA)·심리(NOVA) 분석가의 의견을 종합해 최종 투자 판단(BUY/HOLD/SELL)을 내리는 총괄 PM."
instructions_file = ".claude/agents/chief-pm.md"
tools = ["WebSearch"]
read_only = true
