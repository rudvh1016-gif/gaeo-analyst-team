# 최종 보고 (쉬운 한국어) — 운영 안정화 구간 0~9 · 2026-09-10

> 이 문서는 대표님이 읽는 보고서다. 어려운 말은 쉬운 말로 먼저 쓰고 괄호에 원래 용어를 넣었다.
> "설치됨 / 실제 작동함 / 성과가 검증됨"은 서로 다른 말이다. 이 문서는 셋을 구분해 적는다.
> 상세 진도표는 `STATUS.md`, 계획은 `MASTER_PLAN.md`, 집에서 할 일은 `HOME_PC_CHECKLIST.md`.

## 한 줄 결론

**클로드가 없어도 저장소가 스스로 돌게 만드는 코드와 문서는 다 만들어서 PR #530 에 올렸다.** 아직 두 가지가 남았다.
① 집 컴퓨터의 모의투자 프로그램을 **한 번 손으로** 최신 상태로 맞춰야 다시 돈다(5분). ② 새 자동 점검(`ops-daily`)이 진짜로 도는 것은 **병합 뒤 첫 실행**에서 확인한다.

## 설치됨 · 실제 작동함 · 성과 검증됨 (구분표)

| 것 | 설치됨(코드·문서) | 실제 작동함 | 성과 검증됨 |
|---|---|---|---|
| 시세·자동분석 수집(GitHub Actions) | 이미 있었음 | ✔ 2026-09-10 09:55 / 09:42 갱신 확인 | 해당 없음 |
| 파이프라인 감시(워치독 + 통합 점검 요약) | ✔ 통합 점검 새로 추가 | 워치독 ✔ / 통합 점검 요약은 병합 뒤 첫 run 에서 확인 | 해당 없음 |
| 모의투자 러너 자가 복구(이력 재작성 뒤 스스로 재기준) | ✔ | ✗ 집 PC 가 새 코드를 아직 못 받음 → 집에서 1회 수동 | ✗ (모의투자 성과는 표본 부족) |
| 예정 시험 GitHub 실행기(`ops-daily`, 평일 17:05) | ✔ | 병합 뒤 계획 모드 실행으로 확인 예정. 첫 진짜 시험은 9/15 | 해당 없음 |
| Codex 용 공용 진입점 | ✔ (13 스킬 · 14 역할) | ✗ Codex 실제 인식 미확인 | 해당 없음 |
| 투자검증(사전등록 4가설) | 이미 있었음 + 안전 검사 추가 | 자동 실행은 10/19 | ✗ 표본 0/20 (아직 아무것도 증명되지 않았다) |
| 이력 압축(compact-history) | ✔ 자동 중지 · 수동 가드 | 되돌리기 가능한 결정 | 해당 없음 |

---

## 1. 개오팀 모의투자는 이제 어디서, 언제, 실제로 돌아갔나?

- **어디서**: 집 Windows PC 한 곳(작업 스케줄러 「GAEO Paper Trading」). 평일 09:05~15:05, 30분마다. Oracle 서버로 옮기지 **않았다**(옮길 조건이 안 됐다: 그 서버 자원은 미확인이고, 다른 작업이 같은 서버를 쓰고 있다).
- **실제로 마지막으로 돈 때**: **2026-09-01 15:05**. 9/2 부터 멈췄다.
- **왜 멈췄나**: 9/2 새벽 저장소의 옛 기록(이력)을 통째로 다시 쓰는 자동 작업이 돌았다. 집 PC 의 복사본은 옛 번호를 들고 있어 "공통 조상이 없다"며 매번 동기화에 실패했다(exit 6). 프로그램 오류가 아니라 **두 복사본이 서로 다른 족보를 갖게 된 것**이다.
- **지금 상태**: 고쳐진 프로그램은 "안 올린 기록이 없으면 스스로 새 족보로 갈아탄다"(옛 것은 `refs/gaeo-backup/`에 보관). 다만 **집 PC 는 새 프로그램을 받기 전이라 스스로 못 고친다.** 집에서 한 번 손으로 맞추면(`HOME_PC_CHECKLIST.md` 3절, 5분) 다음 거래일 09:05 부터 다시 돈다.
- **끊긴 기간 처리**: 없는 거래를 만들지 않는다(소급 체결 0). 빈 날은 `dataGaps` 로 기록되고, 보유 종목은 규칙(5일 보유)대로 늦게 정리된다 — 설계된 정상 동작.

## 2. 시세·자동분석·PAPER·Evolution 중 정상/대기/고장/미확인은?

| 것 | 상태 | 근거(2026-09-10) |
|---|---|---|
| 시세(`data.js`) | **정상** | 09:55 갱신(임계 25분 안) |
| 자동분석(`auto_analysis.js`) | **정상** | 09:42 갱신(임계 70분 안) |
| 모의투자(PAPER) | **고장(정지)** | 마지막 회차 9/1 15:05. 집 PC 조치 필요(위 1번) |
| Evolution(주간 연구) | **정상 대기** | 9/6 실행, `BOOTSTRAP_SHADOW`(표본 쌓는 단계), 승격 0 — 정상 |
| 예정 시험 실행기(`ops-daily`) | **미확인** | 병합 뒤 첫 실행에서 확인 |
| 사이트 실물(gaeoteam.com) | **미확인** | 이 작업 환경에서 접속이 막혀 있다 |

## 3. 토큰 없이(AI 없이) 혼자 확인·보고·복구하는 범위는?

**혼자 하는 것(전부 일반 프로그램, LLM 호출 0)**
- 확인: 장중 15분마다 워치독(시세·자동분석 신선도·워크플로 유효성·통합 점검 요약) / 평일 17:05 `ops-daily`(통합 점검·예정 시험·저장소 용량) / 16:30 모의투자 상태 이슈 / 주 1회 Evolution.
- 보고: GitHub Actions 요약 + 제목이 고정된 이슈(같은 사고는 한 번만 쓰고, 정상이면 닫는다). "확인 못 함"은 절대 "정상"으로 적지 않는다.
- 복구(미리 허용한 것만): 멈춘 수집기 취소·재기동, 저장에 계속 실패하는 수집기의 자진 사퇴, 모의투자 러너의 재기준(안 올린 기록이 없을 때만), push 재시도, 예정 시험 실행·기록.

**혼자 못 하는 것(사람 또는 개발 AI 세션)**: 집 PC 켜기·로그인, 토스 허용 IP 재등록, 산식·가중치 변경, 원장 되돌리기, 저장소 이력 압축(사람이 confirm 을 쳐야 함), 코드 수정(프로그램은 **수리 요청서**만 쓴다: `docs/operations/repair_requests/`).

## 4. 기존 하네스와 AI 역할은 무엇을 보존하고 어떻게 가볍게 했나?

- **보존**: 역할 파일 14개(`.claude/agents/`), 스킬(`.claude/skills/`), Evolution 하네스(`docs/GAEO_HARNESS.md`), CI, 워치독, 외부 SEO·디자인 스킬(원문·라이선스 그대로). 지운 것은 없다.
- **가볍게**: `AGENTS.md` 45.7KB → 28.8KB(세부 절 5개를 `docs/rules/*.md` 로 **원문 그대로** 옮기고 대응표 `docs/agent/RULES_MAP.md`). 맨 위에 「작업 지도」(절대규칙 8줄 + 무엇을 읽고 무엇을 검사할지 표). 한 장짜리 `docs/HARNESS.md`. 검사 입구 하나 `python3 gaeo_check.py <묶음>`.
- **AI 부르는 규칙**: 정상 점검은 AI 0명(프로그램이 함) · 일은 메인 1명 · 동시 2명 이내 · 관점은 순서대로 · 모델 지정 없음(자동 배정·라우터 없음). 유지보수 진입점 `/gaeo-maintain` 신설.

## 5. 9/15·9/23·10/19·11/16 시험의 GitHub 일정·실행·결과보존은 어디에 있나?

| 무엇 | 어디 |
|---|---|
| 일정 원본(기계용) | `config/validation_schedule.json` |
| 사람용 표(자동 생성) | `docs/VALIDATION_SCHEDULE.md` |
| 실행 프로그램 | `run_validation_schedule.py` (예정 시각 전 실행 금지 · 하루 1회 · 허용 명령만) |
| 자동 실행 | `.github/workflows/ops-daily.yml` — 평일 17:05 KST(GitHub 사정으로 몇 분 늦을 수 있고, 늦은 시간은 기록에 남는다) |
| 결과 보존 | `docs/audits/validation_runs/` — 원장 `ledger.jsonl`(덧붙이기만) + 결과 JSON + 동결 입력(`.inputs.json.gz`, 나중에 `--replay` 로 같은 판정 재현) + 후속 조치 **명세**(`followup.md`, 코드 변경은 안 함) |
| 알림 | 이슈 「📅 [GAEO Validation] 예정 시험 실행 기록」(실행했거나 사람 확인이 필요할 때만) |
| 표본이 모자라면 | 결론 없이 표본 수만 적고 7일 뒤 다시(최대 3회). 실패 3회면 사람에게 |

- 9/15 DIANA 확인·9/23 FLOW 표본 점검은 **확인 도구**(`check_team_weights_transition.py`·`check_flow_validation_readiness.py`)가 숫자만 낸다. FLOW 6-arm 채점은 정의가 코드로 확정되지 않아 **하지 않는다**(정의 4건은 새 등록이 필요).
- 클로드 예약(Routine) 4건은 **아직 그대로 켜 두었다.** GitHub 실행기가 실제 due 시험을 기록하는 것을 보고 나서 "원장 확인 + 미리 정한 후속만"으로 바꿀 예정(병합 뒤).

## 6. 클로드를 끊었을 때 남는 기능과 아직 별도 확인이 필요한 기능은?

- **남는 것(전부 GitHub·집 PC 에서 돎)**: 시세·자동분석 수집, 워치독, `ops-daily`(예정 시험·통합 점검·용량 보고), 모의투자 상태 이슈, Evolution, 모의투자 러너(집 PC), 규칙 문서와 Codex 진입점.
- **사라지는 것**: ① 매시 안전망 Routine(워치독을 깨우는 보조 신호 — 없어도 워치독 자체는 15분 cron 으로 돌지만 GitHub 지연 시 몇십 분 늦을 수 있다) ② 매일 시황 글 자동 발행(글쓰기라 LLM 필요) ③ 월·금 제안 ④ 시험 결과를 **쉬운 말로 풀어 주는 보고**(원장·이슈에는 남지만 사람이 읽어야 한다) ⑤ 시험 통과 시 후속 조치를 PR 로 옮기는 일(사람 또는 개발 AI 세션이 §3 표대로).
- **별도 확인 필요**: `ops-daily` 첫 실행 · Codex 실기동 · 사이트 실물 · 집 PC 러너 재가동.

## 7. Codex 스킬·역할·규칙은 어디까지 확인됐나?

| 단계 | 상태 |
|---|---|
| 파일 준비 | ✔ `.agents/skills/<13개>/SKILL.md`(원본 경로 + 대응표) · `.codex/agents/<14개>.toml` · `docs/agent/ROLES.md` · `docs/agent/CODEX_SCENARIOS.md`(시나리오 4개) · 생성기 `sync_agent_compat.py` |
| 정적 검사 | ✔ `test_agent_compat.py` 11건(원본과 동기·이름 일치·절차 복제 없음·참조 파일 존재·모델 지정 없음) |
| 실제 앱 확인 | ✗ **미확인**. Codex 공식 문서가 이 작업 환경에서 접속 차단. `.toml` 형식은 "형식 미확인"이라고 파일에 적어 두었다. Codex 가 있는 컴퓨터에서 `CODEX_SCENARIOS.md` §5 표를 채워야 확인이다 |

## 8. 투자검증에서 무엇을 보강했고, 전략·사전등록·과거 성과는 그대로인가?

- **그대로(변경 0)**: BUY/HOLD/SELL 산식, 가중치, 임계값, 사전등록 상수·가설·절차, 등록된 테스트, 과거 판단 기록, 모의투자 원장, `tickers.js`.
- **보강**: ① GAP 표 `docs/operations/INVESTMENT_VALIDATION_GAP.md` — 11항목을 "코드 있음/연결됨/기록 있음/표본 충분/효과 증명" 다섯 칸으로 나눠 적었다. **효과 증명은 전부 ✗** (사전등록 익은 판단일 0/20, FLOW 공통 날짜 12/20, DIANA 채점 0일). ② 음성 대조 검사(`test_validation_negative_control.py`) — 결과와 무관한 가짜 자료를 넣으면 어떤 가설도 "통과"가 나오지 않는지, 같은 입력이면 같은 결과가 나오는지, 일정 파일의 최소 표본(20/40일)·날짜가 등록과 같은지 잠근다. ③ 시험 실행기의 동결 입력·재현.
- 실자료로 새 효과를 계산하지 **않았다**(등록 규칙: 표본 부족이면 다른 방법으로 계산 금지).

## 9. 추가 과금 경로는 없나? 확인 못 한 무료 한도·운영 제약은?

- **새 유료 경로 0**: 새 API·서버·크레딧·결제 없음. 모든 자동화는 GitHub Actions(저장소에 이미 있던 방식)와 `GITHUB_TOKEN` 만 쓴다. `ops-daily` 는 하루 1회 2~3분짜리 잡 하나가 늘었다. 이력 압축은 자동으로 돌지 않는다.
- **확인 못 한 것**: ① GitHub Actions 무료 분·저장 공간의 실제 사용량(계정 화면에서만 보인다) ② 저장소 크기 — 지금 **1.88 GiB**, 최근 7일에 543 MB 늘었다(≈ 한 달 2.3 GB). 이대로면 12월쯤 GitHub 권장 상한 5 GB 근처. `ops-daily` 가 매일 크기를 적고 3 GiB·4.5 GiB 에서 표시한다. 줄이는 방법은 소유자 결정(`docs/HISTORY_PRESERVATION.md` §5) ③ Oracle 서버 자원(미확인, 그래서 안 옮김) ④ 집 PC 전원·로그인·공인 IP(토스) 의존.

## 10. PR·커밋·배포·검사 근거와 다음 담당자의 정확한 첫 행동은?

- **PR**: [#530](https://github.com/rudvh1016-gif/gaeo-analyst-team/pull/530) (브랜치 `claude/auto-analysis-failure-prevention-94we7m`, 기준 main `a003e02bda`).
- **커밋(구간별)**: 0 `d44c2ca20d` · 1 `2385a9e907` · 2 `694f2d4d41` · 3·4 `54a830ae64` · 5 `cec7d758fb` · 6 `a46fc377cf` · 7·8 `3021bae486` · 9(이 보고서) 아래 STATUS 참조.
- **검사**: `python3 gaeo_check.py premerge`(CI 와 같은 것) **123 PASS / 0 FAIL**(파이썬 84 + 노드 39) · 브라우저 smoke 2건 PASS · 실데이터 계획 모드 예행(9/15·9/23·10/19) 정상. CI(`ci.yml`) 결과는 PR #530 에 붙는다.
- **배포**: main 병합 = GitHub Pages 배포(별도 단계 없음). 병합 뒤 `python3 gaeo_check.py postdeploy` 와 `ops-daily` 계획 모드 실행으로 확인한다(사이트 실물은 이 환경에서 확인 불가).
- **다음 담당자의 첫 행동(순서대로)**
  1. 집 PC: `docs/operations/HOME_PC_CHECKLIST.md` 3절(러너 재기준) + 8절(개발용 저장소 재기준). 5분.
  2. 병합 뒤: GitHub → Actions → `ops-daily` → Run workflow(apply 체크 안 함) → 요약에 「예정 시험 실행기」 계획(NOT_DUE 4건)과 「통합 상태」가 나오는지 본다.
  3. 9/15(월) 17:05 뒤: `docs/audits/validation_runs/ledger.jsonl` 에 `VS-20260915-DIANA-SHRINKAGE-CHECK` 한 줄이 생겼는지 본다. 생기면 클로드 Routine 4건을 "원장 확인 + 후속만"으로 바꾼다.
  4. 언제든: `python3 gaeo_check.py preflight` → `docs/operations/STATUS.md` → `/gaeo-maintain`.
