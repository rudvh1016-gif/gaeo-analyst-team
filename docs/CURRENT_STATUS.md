# 역사 기록: 2026-07-27 상태 스냅샷 — 개오(Gaeo) 애널리스트팀

> 이 문서는 현재 상태 문서가 아니라 당시 기록이다. 숫자와 설명을 오늘의 서비스 사실로
> 인용하지 않는다. 작성 시점: 2026-07-27,
> 브랜치 `claude/latest-analyst-file-data-a7e4y1`, 커밋 `b417203`. 구조 자체가 궁금하면
> [ARCHITECTURE.md](./ARCHITECTURE.md)를 본다 — 이 문서는 "숫자"와 "지금 뭐가 되어있는지"만 다룬다.

> 2026-09-02 보안 후속 메모: 아래의 방문자 KVdb 게시판 쓰기와 브라우저 GitHub PAT 발행은
> 당시 상태를 기록한 항목이다. 현재 공개 페이지에서는 둘 다 제거됐고, 운영자 콘텐츠는
> 로컬 초안을 PR과 CI로 검토한 뒤 발행한다. 현재 구조는 `ARCHITECTURE.md`를 따른다.

## 규모

| 항목 | 수치 |
|---|---|
| 추적 종목(tickers.js) | 500개 |
| 정밀분석 대상 종목(analysis.js) | 14개 |
| 자동분석 대상 종목 | 500개 전체(정밀분석 14개 포함) |
| 뉴스분석 글(news_analysis.js) | 17건 |
| 종목공부 글(stock_study.js) | 33건 |
| 주식공부 글(stock_lessons.js) | 34건 |
| 부동산공부 글(estate_lessons.js) | 15건 |
| 계산기(calculators.js) | 7종 |

## 정밀분석 14종목 (analysis.js에 있는 종목)

267260 · 402340 · 475150 · 080220 · 005930 · 000660 · 009150 · 009155 · 011070 ·
010120 · 005380 · 042700 · 007660 · 017670

## 최근 완료된 주요 작업 (2026-07 하순)

- Home Dashboard를 Gaeo Master Design으로 리디자인 — Emerald 브랜드 시스템, 상단 고정
  글로벌 네비게이션, PC 와이드 레이아웃, 모바일 전체 메뉴를 적용. 기존 사이드바 기능은
  상단 전체 메뉴 패널로 이동해 API·데이터·기능을 그대로 유지.
- 첫 화면에 최신 글 5개, 브리핑의 관련 업종·뉴스 바로가기, 관심종목의 최근 거래일 변화 요약을 추가.
- 🔍 종목 스크리너에 빨간색 "기준일시" 표시 추가 — 데이터가 언제 기준인지 혼동 방지.
- SK하이닉스 실적발표 D-4 + 최태원 회장 재산분할 판결 관련 뉴스분석 기사 발행.
- 부동산 취득세 완전정복 기사 발행(중복 주제 회피를 위해 사용자가 직접 대체 주제 선택).
- 마케팅 문구(제목·메타설명·about.html·manifest.json) 전면 수정 — "정밀분석/딥다이브"
  언급을 모두 제거하고 "500종목 매일 자동분석"으로 정직하게 재정리(실제로 정밀분석은
  거의 수행되지 않기 때문).
- og-image.png(공유 미리보기 이미지) 재생성 — 옛 태그라인이 이미지 픽셀에 박혀있던
  문제를 발견하고 새 카드로 교체(카카오톡/스레드 링크 미리보기용).
- 카카오 AdFit 광고 슬롯을 사이드바(분석 시작 버튼 아래, 업종 폴더 위)에 추가.
- 이재명 대통령 美 빅테크 CEO 회동·SK-엔비디아 5,000억 달러 파트너십 기사 발행.
- 월요일(7/27) 코스피 개장 전 체크리스트 기사 발행(주말 뉴스 종합 + 이번 주 캘린더).
- 스레드(Threads) 홍보 문구 스타일을 durable 규칙으로 확정: 마침표 없이, AI 말투 대신
  자연스러운 사람 말투로, 글 맨 아래에 `https://gaeoteam.com/snap/<mode>/<id>.html` 정적 canonical 링크 첨부.
- 커뮤니티 게시판 kvdb.io 백엔드 보안 검토 — 게시글 삭제 비밀번호 확인이 클라이언트에서만
  이뤄지는 구조적 한계를 발견(영향 범위는 게시판 콘텐츠로 한정, 저장형 XSS는 없음).
- 이 문서 세트(AGENTS.md 신설, CLAUDE.md 재정리, docs/ 4종 신설) — Claude Code와 Codex가
  번갈아 작업하는 새 워크플로에 맞춰 문서 구조를 재편.

## 알려진 이슈 / 유의사항

- **README.md는 낡았다.** 2026-07-09 시점(9종목·로컬 cron·GitHub Pages 배포 이전) 기준으로
  멈춰 있어 지금 구조와 전혀 다르다. 아직 수정 요청은 없었으므로 그대로 두되, 최신 정보의
  출처로 참고하지 않는다 — `AGENTS.md`/`CLAUDE.md`/`docs/`와 실제 코드가 기준이다.
- **당시에는 커뮤니티 게시판 비밀번호 확인이 클라이언트 사이드뿐**이었다. 2026-09-02에
  공개 방문자 쓰기를 닫아 이 경로를 제거했다.
- **당시에는 관리자 발행용 GitHub 토큰이 localStorage에 저장**됐다. 2026-09-02에 토큰 입력,
  저장, Contents API 직접 쓰기를 제거하고 PR 발행 요청 복사로 바꿨다.
- 이 원격 세션 환경에서는 네이버 금융 접속이 403으로 막혀 있어, 이 세션에서 직접 시세를
  수집할 수 없다(러너에게 위임해야 함 — WORKFLOW.md 5번 참고). 이건 이 특정 원격 세션의
  제약이라 Codex가 다른 환경에서 작업할 때는 해당하지 않을 수 있다.

## 진행 중 / 다음 단계

- Home Dashboard의 Gaeo Master Design을 사용자에게 검토받은 뒤, 승인되면 같은 디자인
  시스템으로 나머지 페이지를 순차 리팩터링한다. 페이지별로 다른 디자인을 새로 만들지 않는다.
- `docs/BRANCH_GOVERNANCE_PLAN.md`에 따라 생성 데이터 writer를 분리한 뒤 main ruleset을 단계적으로 도입한다.

---

## 🔖 다음 세션 인수인계 (2026-09-08 17:5x KST 작성 · 주간 한도 소진으로 중단)

내일 아침 이 절부터 읽으면 바로 이어서 할 수 있다. **앞부분을 다시 조사할 필요 없다.**

### 오늘 끝낸 것 (전부 병합됨)

| PR | 내용 | 상태 |
|---|---|---|
| #519 | fetch 탈출구 + `branches: [main]` 가드 + `test_ci_parity.py` | 병합 |
| #521 | 워치독 요약 줄 정직화(판정 보류를 "정상"이라 말하지 않기) | 병합 |
| #522 | 9/8 시황 발행 | 병합 |
| #523 | 9/8 오진 1차 기록 | 병합 |
| #524 | **원인 확정 + 수집기 자진사퇴 장치 + #523 정정** | 병합 |

main HEAD는 `1fffe377`. 전체 테스트 1,079건 통과.

### 바로 이어서 할 일 — 우선순위 순

**① `update-analysis.yml`에 자진사퇴 장치 넣기 (P0 · 오늘 못 한 것)**

`update-prices.yml`에는 "3사이클 연속 저장 실패 시 스스로 물러남"을 넣었지만,
`update-analysis.yml`에는 **못 넣었다.** 큰 `run:` 블록이 20,220B로 안전선(20,500)까지
**280B**밖에 안 남아서다. 같은 코드는 약 417B 필요하다.

절차:
1. 큰 블록에서 **한 덩어리를 별도 스텝으로 분리**해 공간을 만든다. 전례가 있다 —
   거래일 판정을 별도 스텝으로 빼고 `$GITHUB_ENV`로 결과만 넘긴 방식
   (`update-prices.yml`의 "거래일 판정" 스텝 참고).
2. `python3 -m unittest test_workflow_size`로 **크기를 먼저 재고** 진행한다.
   ⚠️ 2026-09-07에 크기를 안 재고 넣었다가 파일 전체가 무효가 됐다.
3. `update-prices.yml`의 `failed_pushes` / `MAX_FAILED_PUSHES` / `pushed` 플래그 블록을
   그대로 옮긴다.
4. `test_workflow_size.CollectorWithdrawsWhenItCannotSave`의 대상에
   `update-analysis.yml`을 추가한다(지금은 `update-prices.yml`만 검사한다).

**② Task #52 — 성적표 정적 URL 발행 파이프라인** → **2026-09-09 배관 완료.**

앱의 성적표 화면은 숫자를 매번 다시 계산해서 링크로 남길 수도, 인용할 수도 없다.
그래서 발행 시점 숫자를 **동결해** 영구 주소에 박는 경로를 새로 깔았다.

| 무엇 | 어디 |
|---|---|
| 주소 | `https://gaeoteam.com/snap/report/<id>.html` (새 `report` 모드) |
| 데이터 | `scorecard_reports.js` — append-only, `frozen` 블록에 동결 숫자 |
| 동결 | `publish_scorecard_report.py` — 평가 리포트에서 뽑아 초안을 찍는다 |
| 계약 | `test_scorecard_report_pipeline.js`(8건) · `test_scorecard_report_publish.py`(7건) |

되돌아가는 링크는 `?mode=report&id=N`이 아니라 **앱의 기존 성적표 화면**(`?mode=scorecard`)이다
— 앱에 그 상세 화면이 없어서 보내면 빈 화면이 뜬다.

**아직 발행하지 않았다.** 첫 호는 10/19 사전등록 확정 평가이고, 그 전에 결과를 글로
내보내면 등록이 소멸한다. 평가 스크립트가 판단일 20일 미만이면 효과 크기를 안 내주고,
`publish_scorecard_report.py`도 `status != EVALUATED`면 초안 자체를 거부한다(문이 두 겹).

10/19에 할 일: `python3 publish_scorecard_report.py --draft` → 나온 뼈대를
`scorecard_reports.js`에 붙여 넣고 **본문만** 사람이 쓴다(숫자는 `frozen`에 있는 것만).
→ `python3 seo_publish_gate.py` 통과 확인 → PR.

**③ Task #26 — 커뮤니티 Cloudflare 글쓰기 재개**

큰 작업. 한도가 넉넉한 날에 착수.

### 확정된 사실 (다시 조사하지 말 것)

- **9/7·9/8 오후 시세 공백의 원인**: 러너가 GitHub과의 통신을 잃은 채 네이버 수집만
  계속 성공했다. 9/8 14:14~15:16의 5사이클 약 62분 동안 수집은 전부 성공, 저장은 전부 실패.
  근거는 run `34183774372`(job `101943120530`) 로그 전체. 자세한 것은
  `docs/PIPELINE_WATCHDOG.md`의 2026-09-08 절.
- **정상 시세 저장 간격은 11~13분**이다(6거래일 202개 중 194개, 96%).
  한 사이클의 구조적 최악은 약 31분이므로, **31분을 넘는 공백은 "느린 사이클"이 아니라
  "여러 사이클이 연달아 저장 실패"다.** 이 기준을 진단에 쓸 것.
- **매시 안전망 Routine은 v6**이다. 사람(Claude)이 직접 run을 취소하지 않는다.

### 아직 모르는 것 (단정 금지)

- 러너가 **왜** GitHub과의 통신을 잃었는지. 후속 run은 같은 시각에 정상 push했으므로
  GitHub 전면 장애는 아니었다. 그 이상은 확인되지 않았다.
- 9/7의 84분 공백이 9/8과 같은 원인인지. **로그를 확인하지 않았다.**

### 예정된 Routine (건드리지 말 것)

`9/15` DIANA 채점 전환 확인 · `9/23` FLOW 산식 검증 + BUY 표본 점검 ·
`10/19` BUY 필터 확정 평가 · `11/16` H1 40판단일 재확인.
모두 사전등록 규칙(`docs/PREREGISTRATION_BUY_FILTERS_20260905.md`)을 따른다 —
**결과를 보기 전에 산식을 손대면 등록이 소멸한다.**
